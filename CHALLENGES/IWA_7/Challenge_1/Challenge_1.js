const value = "3"
console.log(Number(value) + 4 + Number(value)) // added Number() to convert value from string to number. 
// value is a string. adding a string to a number doesn't give you the sum but concatenate them. 